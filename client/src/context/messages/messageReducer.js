import {
  ADD_MESSAGE,
  DELETE_MESSAGE,
  SET_CURRENT,
  CLEAR_CURRENT,
  UPDATE_MESSAGE,
  FILTER_MESSAGES,
  CLEAR_FILTER,
  MESSAGE_ERROR,
  GET_MESSAGES,
  CLEAR_MESSAGES,
} from "../types";

export default (state, action) => {
  switch (action.type) {
  }
};
